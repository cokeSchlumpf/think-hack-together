<server description="liberty-maven-bluemix-sample-server">
  <featureManager>
    <feature>jaxrs-2.0</feature>
  </featureManager>

  <httpEndpoint id="defaultHttpEndpoint" httpPort="${Port}" />

  <application id="think-hack-together" location="think-hack-together.war" name="think-hack-together" type="war" context-root="/" />
</server>
