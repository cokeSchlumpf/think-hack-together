import React from 'react';
import ReactDOM from 'react-dom';
import Router from 'react-router';

import routes from './components/application';

ReactDOM.render(routes, document.getElementsByTagName('div')[0]);
